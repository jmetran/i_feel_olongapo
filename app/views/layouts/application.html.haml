<!DOCTYPE html>

%html
  %head
    %title= yield_for(:title)
    = display_meta(:title => yield_for(:title), :keywords => yield_for(:keywords), :description => yield_for(:description) )
    = stylesheet_link_tag    "application"
    = javascript_include_tag "application"
    = csrf_meta_tags
  %body
    = yield